# Stage 1 KDE6 migration report

- status: DONE_WITH_CONCERNS
- commits: `946f0ab9f0381479d08bfa8d069b08e9c34c8e40` (`build: split Breeze10 KDE6 plugins`)
- files changed: `CMakeLists.txt`, `libbreezecommon/CMakeLists.txt`, `breezedecoration.cpp`, `config/CMakeLists.txt`, `config/kcm_breeze10.cpp`, `config/kcm_breeze10.json`
- configure command output summary: `cmake -S . -B build-kde6-stage1 -DCMAKE_BUILD_TYPE=Debug -DBUILD_TESTING=ON` reached ECM, then failed because KF6 components were unavailable. CMake reported missing `CoreAddons GuiAddons ConfigWidgets WindowSystem I18n KCMUtils IconThemes ConfigGui` and their package config directories. It also emitted `fatal: no upstream configured for branch 'feature/kde6-migration'` from KDE CMake settings. The optional XCB and target generation could not be inspected because configuration stopped before dependency completion.
- concerns: The environment has no usable KF6/KDecoration package set, so compile and generated-target validation are blocked. The installed ECM does not provide the legacy `KConfig`, `KCoreAddons`, or `KCMUtils` include modules; those unnecessary includes were removed. Legacy desktop files remain on disk and are not installed by the new rules. `breeze.json` was unchanged because it already contains the required decoration identity and `kcmodule` association.
- Stage 2 notes: decoration target is `breeze10`; common library target is `breeze10common6`; KCM target is `kcm_breeze10`. The internal KDecoration selection is `BREEZE_KDECORATION_TARGET`, preferring `KDecoration2::KDecoration` and falling back to `KDecoration3::KDecoration` with `BREEZE10_HAVE_KDECORATION3`. Decoration installation uses `KDECORATION_PLUGIN_DIR`, falling back to `${KDE_INSTALL_PLUGINDIR}/org.kde.kdecoration2`; KCM installation uses `KDECORATION_KCM_PLUGIN_DIR`, with the same fallback namespace. XCB remains optional and is linked only when `BREEZE_HAVE_X11` is true.
